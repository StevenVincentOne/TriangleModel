export default class Size {
    width: number;
    height: number;
    constructor(width: number, height: number);
    add(size: Size): Size;
    readonly transform: string;
    static instance(width: any, height?: number): Size;
}
